{
  "name": "gestfrota",
  "version": "1.0.0",
  "private": true,
  "description": "GestFrota - Sistema de Gestão de Frotas",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"cd api && npm run dev:local\" \"cd web && npm run dev\"",
    "install:all": "npm install && cd api && npm install && cd ../web && npm install",
    "build:all": "cd api && npm run build && cd ../web && npm run build",
    "api:dev": "cd api && npm run dev:local",
    "api:dev:prod": "cd api && npm run dev",
    "api:build": "cd api && npm run build",
    "api:start": "cd api && npm start",
    "api:migrate": "cd api && npm run prisma:migrate:local",
    "api:migrate:prod": "cd api && npm run prisma:migrate",
    "api:seed": "cd api && npm run seed",
    "web:dev": "cd web && npm run dev",
    "web:build": "cd web && npm run build",
    "web:preview": "cd web && npm run preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}